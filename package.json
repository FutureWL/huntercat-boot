{
  "name": "pjd-vcn-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @pjd/web dev",
    "build": "pnpm --filter @pjd/web build",
    "start": "pnpm --filter @pjd/web start",
    "dev:api": "pnpm --filter @pjd/api dev",
    "build:api": "pnpm --filter @pjd/api build",
    "start:api": "pnpm --filter @pjd/api start",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
