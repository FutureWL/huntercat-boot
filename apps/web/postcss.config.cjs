module.exports = {
  // PostCSS 插件配置：用于构建阶段处理 CSS
  plugins: {
    // Tailwind CSS：根据使用到的 class 生成对应的 CSS 规则
    tailwindcss: {},
    // Autoprefixer：为 CSS 自动补齐浏览器前缀（提升兼容性）
    autoprefixer: {},
  },
}
